apiVersion: v1
kind: Pod

metadata:
  name: web-pod
  namespace: k8s-lab
  labels:
    app: web
    environment: learning

spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - name: http
          containerPort: 80
