apiVersion: v1
kind: Service

metadata:
  name: web-nodeport
  namespace: k8s-lab

spec:
  selector:
    app: web

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort
